export interface InitMultimodalModelResult {
    model: MultimodalModel;
    elapsed: number;
}
export declare class MultimodalModel {
    static create: (id: string, proxy?: boolean) => Promise<InitMultimodalModelResult>;
}
