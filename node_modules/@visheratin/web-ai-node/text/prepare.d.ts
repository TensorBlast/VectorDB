import * as ort from "onnxruntime-common";
import { Tokenizer } from "../common.js";
export declare const prepareTextTensors: (inputs: string[], tokenizer: Tokenizer, addSpecialTokens: boolean, padTokenID: number, bosTokenID?: number) => Promise<ort.TypedTensor<"int64">[]>;
