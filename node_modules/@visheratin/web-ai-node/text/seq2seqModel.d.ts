import { TextMetadata } from "./metadata.js";
import { TextProcessingResult } from "./interfaces.js";
import { BaseTextModel } from "./base.js";
export type Seq2SeqResult = TextProcessingResult & {
    text: string[];
};
export declare class Seq2SeqModel extends BaseTextModel {
    private encoder?;
    private decoder?;
    private cache;
    constructor(metadata: TextMetadata);
    init: (proxy?: boolean) => Promise<number>;
    process: (inputs: string | string[], prefix?: string) => Promise<Seq2SeqResult>;
    processStream(inputs: string | string[], prefix?: string): AsyncIterable<string[]>;
}
