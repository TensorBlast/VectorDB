import { TextMetadata } from "./metadata.js";
import { TextProcessingResult } from "./interfaces.js";
import { BaseTextModel } from "./base.js";
export type FeatureExtractionResult = TextProcessingResult & {
    result: number[] | number[][];
};
export declare class FeatureExtractionModel extends BaseTextModel {
    private model?;
    private dense?;
    private cache;
    constructor(metadata: TextMetadata);
    init: (proxy?: boolean) => Promise<number>;
    process: (inputs: string | string[]) => Promise<FeatureExtractionResult>;
    private generate_output;
    private run_dense;
    private normalize;
}
