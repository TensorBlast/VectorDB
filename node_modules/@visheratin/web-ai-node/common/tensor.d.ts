import * as ort from "onnxruntime-common";
export declare class Tensor {
    ortTensor: ort.Tensor;
    constructor(ortTensor: ort.Tensor);
    at: (indices: number[]) => string | number | bigint;
    setAt: (indices: number[], value: number) => void;
    argmax: (dim: number) => Tensor;
    argmaxIter: (dim: number, targetDim: number, indices: number[], argmax: Tensor, maxValues: Tensor, depth: number) => void;
}
