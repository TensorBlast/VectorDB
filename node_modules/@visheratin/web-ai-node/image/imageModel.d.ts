export interface InitImageModelResult {
    model: ImageModel;
    elapsed: number;
}
export declare class ImageModel {
    static create: (id: string, proxy?: boolean) => Promise<InitImageModelResult>;
}
