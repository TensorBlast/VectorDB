import * as ort from "onnxruntime-node";
import { SessionParameters } from "../common.js";
export declare class Session {
    ortSession: ort.InferenceSession | undefined;
    cacheSize: number;
    params: SessionParameters;
    constructor(params: SessionParameters);
    init: (modelPath: string) => Promise<void>;
    fetchData: (modelPath: string) => Promise<ArrayBuffer>;
    run: (input: ort.InferenceSession.OnnxValueMapType) => Promise<ort.InferenceSession.OnnxValueMapType>;
    inputNames: () => readonly string[];
    outputNames: () => readonly string[];
}
