import { WasmTokenizer } from "@visheratin/tokenizers-node";
export declare class Tokenizer {
    instance: WasmTokenizer | undefined;
    private tokenizerPath;
    constructor(tokenizerPath: string);
    init(): Promise<void>;
    decode(ids: Uint32Array, skip_special_tokens: boolean): Promise<string>;
    encode(text: string, add_special_tokens: boolean): Promise<Uint32Array>;
}
